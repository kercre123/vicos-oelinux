# BT Oopen source Packages
IMAGE_INSTALL += "bt-property"
# For Victor, we don't need Qualcomm's bt-app (It is just a demo)
# IMAGE_INSTALL += "bt-app"
IMAGE_INSTALL += "fluoride"
IMAGE_INSTALL += "libbt-vendor"